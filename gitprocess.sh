git init
git remote add origin git@github.com:username/new_repo
git add filenames++
git commit -m "commit message"
git push origin <branch name>

checkout another branch --> git checkout <branch name>

checkout new branch --> git checkout -b <new branch name> 
